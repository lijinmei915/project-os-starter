---
layer: knowledge
type: spec
last_verified: 2026-06-04
---

# 环境说明

> 用途：说明本地运行、依赖、环境变量和外部服务。
> 什么时候更新：启动命令、依赖版本、环境变量、外部服务或账号权限变化时。
> 不要写什么：产品路线、交接流水、一次性调试日志。

## 本地运行

```bash
# TODO: 写启动命令
```

## 依赖

| 依赖 | 版本 / 要求 | 说明 |
|------|-------------|------|
| `未记录` | `未记录` | `未记录` |

## 环境变量

| 变量 | 必填 | 用途 | 示例 |
|------|------|------|------|
| `DEEPSEEK_API_KEY` | 否 | 接入 DeepSeek 时使用的本地 API key | 不要写真实值 |
| `未记录` | `未记录` | `未记录` | `未记录` |

本地密钥使用方式：

```bash
cp .env.example .env.local
```

然后只在 `.env.local` 中填写真实值。`.env.local`、`.env` 和其他 `.env.*` 文件不能提交到 git。

检查本地密钥是否安全：

```bash
bash scripts/check-secrets.sh .
```

## 外部服务

| 服务 | 用途 | 权限 / 账号 |
|------|------|-------------|
| `未记录` | `未记录` | `未记录` |

## 常见环境问题

- `未记录`

## 相关文件

| 文件 | 关系 |
|------|------|
| `docs/ARCHITECTURE.md` | 系统结构决定运行方式 |
| `docs/RUNBOOK.md` | 常见操作和故障处理 |
| `docs/TESTING.md` | 测试命令也依赖环境配置 |
