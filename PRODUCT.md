---
layer: knowledge
type: spec
last_verified: 2026-07-21
depends_on: [PROJECT.md, docs/PRODUCT_PLAN.md, docs/DESIGN_STANDARDS.md]
teaches: "OmniDesk 的产品定位、目标用户、核心价值与体验边界"
use_when: "AI 需要判断某项功能是否服务 OmniDesk 用户、产品价值或体验取舍时"
---

# OmniDesk 产品定义

> 用途：定义 OmniDesk 是什么、为谁服务，以及产品明确不做什么。
> 什么时候更新：目标用户、核心价值、产品边界或体验原则发生变化时。
> 不要写什么：启动命令、运行时实现、当前交接流水或旧 Project OS 分发流程。

## 产品定位

OmniDesk 是本地优先的 AI 工程工作台。它让开发者在一个桌面应用内接入真实工程，以持续对话驱动计划、Patch 草稿、独立审批、受控检查和有限修复，并保留任务为什么成功或失败的完整证据。

它不是 AI 工程评分器、文档模板生成器、跨工具安装包或通用项目初始化向导。

## 目标用户

- 使用 AI 协助维护真实本地工程的独立开发者。
- 需要在写入、终端和检查前保留明确控制权的小团队技术负责人。
- 需要让下一次会话能理解任务上下文、授权范围和执行证据的工程维护者。

用户的核心工作不是“生成一段代码”，而是安全地完成一次可复核的工程变更：理解任务、确定范围、审阅草稿、批准写入、运行检查、处理失败并保留结论。

## 核心价值

1. **可控执行**：模型可以提出 Patch 和检查建议，但工程写入与检查始终需要独立审批。
2. **可恢复任务**：任务中断后从已持久化阶段继续，不伪称继续原 Provider 流，也不自动重放未确认操作。
3. **可审计结果**：任务记录草稿、授权文件、审批、应用结果、检查输出、修复轮次和最终原因。
4. **本地工程优先**：项目、密钥和执行边界留在用户控制的桌面 Runtime；浏览器 Preview 只用于只读预览。

## 产品边界

- 不把 Provider 返回成功显示为任务完成；只有 Patch、审批、检查和最终证据闭环后才可结案。
- 不以“自动化”为名绕过用户的文件写入或命令执行确认。
- 不扩展旧 Project OS 的 CLI、模板分发、跨工具 adapter、工程评分或报告能力。
- 不在当前阶段引入远程执行、云同步、多 Agent 编排或新的管理型信息架构。

## 体验原则

1. **先说明当前状态**：用户能立即看到任务是否只是在生成草稿、等待审批、检查中、可修复失败或已完成。
2. **证据与操作在同一上下文**：批准、检查、修复和失败原因属于同一任务时间线，不分散成脱离上下文的新任务。
3. **默认安全，按需展开**：常用操作保持直接；授权范围、Diff、检查输出和运行证据可展开查看。
4. **桌面优先，Preview 诚实降级**：只读 Preview 不模拟文件写入、终端或恢复能力。
5. **工程工具的克制感**：面向扫描、比较和重复操作，保持紧凑、明确、稳定，而不是营销式页面。

## 成功标准

用户能够完成并理解一次真实工程任务：知道模型做了什么、是否写入、谁批准、检查是否通过、失败时为何失败以及是否还能继续。

产品阶段、交付门槛和后续路线由 `PROJECT.md` 与 `docs/PRODUCT_PLAN.md` 负责；架构和运行边界由 `AGENTS.md` 与 `docs/ARCHITECTURE.md` 负责。
