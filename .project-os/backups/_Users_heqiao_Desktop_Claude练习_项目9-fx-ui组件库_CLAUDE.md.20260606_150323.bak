# Claude Code Adapter — fx-ui

> Claude Code 进入 fx-ui 的专属行为规则。
> 通用规则以 `AGENTS.md` 为准，本文件只处理 Claude Code 特有行为。

---

## ⚡ 进入项目必读（每次新会话）

1. 读 `AGENTS.md` — AI 红线（核心）
2. 读 `PROJECT.md` — 当前进度
3. 读 `HANDOFF.md` — 上次做到哪

**不读这三个，不要动手。**

---

## 路由规则（Claude Code 里的意图分流）

收到任何请求，**先按下面的路由判断**，再执行：

```
要做一个页面 / 生成列表 / 新增功能  →  先查 AGENTS.md 红线 + docs/TOKENS.md
要用某个组件                        →  npx shadcn add <组件名>，不手写
要改颜色 / 样式                     →  查 docs/TOKENS.md，改 theme/fx-theme.css 变量
不确定当前该做什么                   →  读 PROJECT.md + HANDOFF.md
```

**不要触发通用澄清流程（如 intent-clarifier）再问技术栈。**
fx-ui 的技术栈已经锁定：React + TypeScript + Tailwind v4 + shadcn/ui（open-code）。
进来就知道答案，不需要再问用户。

---

## 状态速览

- **上次做到**：Vite + shadcn 工程已搭建，Button 组件换肤验证通过（公司橙 ✅）
- **下一步**：继续拉更多 shadcn 组件，搭建组件展示页
