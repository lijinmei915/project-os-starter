# 当前交接 (Handoff to Next AI)

> 用途：记录当前任务交接、下一步建议、风险和最近上下文。
> 什么时候更新：每次完成一组连续任务、当前状态变化或下一位 AI 需要接手时。
> 不要写什么：长期路线图、完整产品介绍、详细架构说明或变更流水账。

> 上一轮任务：深度重构 Project OS 为全功能 AI Engineering Kit。
> 下一轮重点：真实老项目校准与 Skill 鲁棒性验证。

## 🤖 AI 助手上手简报 (Copy & Paste)

**项目背景**：这是一个名为 Project OS 的 AI 工程化套件，目前已进化到 v0.3。
**核心入口**：
1. **`AGENTS.md`**：阅读我的行为边界和文档规则。
2. **`PROJECT.md`**：了解项目当前所处的阶段（自身工程化已完结，进入校准期）。
3. **`.ai/rules/`**：这里是你的唯一真相源 (SSOT)，所有 docs 均映射于此。
4. **`./kit`**：这是你的 CLI 中控台。

**你可以执行的技能**：
- 输入 `自动反思`：我会抓取 Git 改动并沉淀教训到 `LESSONS.md`。
- 输入 `优化规则`：我会帮你清理冲突和冗余的规则。
- 输入 `做个体检`：执行 v0.3 动态评分模型（包含 Test/Lint 真实检测）。

---

## 🏗️ 本次已完成 (核心成果)

- **AI 统一枢纽**：建立 `.ai/` 目录，通过 `scripts/sync-ai-rules.sh` 实现 100% 动态软链接映射。
- **v0.3 评分模型**：从静态文档检查升级为“技术健康 + AI 效率 + 知识演进”的多维评分。
- **可视化向导**：在 `index.html` 实现“人格化访谈”UI，支持意图驱动的资产自动联动勾选。
- **工程契约向导**：将“AI 意图访谈”收敛为 2 个用户可理解的问题，并按“项目入口 / 工程运行 / 代码结构 / 质量保障 / AI 支持文件”5 层输出工程契约；原 Q2 关注点和 Q3 生成方案已合并为“这次你想先拿到什么”。Q2 现在是单选，并由看清方向 / 先有页面 / 能运行起来 / 方便测试和交接 / AI 工程支持 / 知识库 RAG 一一映射到生成方案、代码结构、AI 支持强度和推荐文档勾选。
- **工程契约布局收口**：右侧生成区已从多个并列模块收敛为 `ContractRecommendation` + `ContractLayerStack` + `ContractBlueprintPreview`；推荐卡片只保留标题和说明，不再显示摘要标签；各层固定展开，不再显示折叠按钮；层头统一为“层名 + 说明 + 计数”，说明文独占第二行并可横跨整个层头宽度，层内小节标题降一级展示，不再重复展示文件 chips；AI 支持层不显示 `1/6` 这类可勾选计数，改为“自动包含 / 按需生成”状态；必选资料、推荐文档和 AI 支持文件按 5 层归属展示，底部预览区只保留文件树和生成后第一步，不再常驻展示额外确认文案、四张设置卡或关键文件用途列表。
- **技术栈文档边界**：新增 `docs/TECH_STACK.md` 模板并放入工程运行层；仅在“能运行起来 / 前端 / 后端 / 全栈 / Agent / RAG”等工程落地方案中自动推荐。`DATA_MODEL.md` 暂不作为默认生成项，避免产品方向或页面原型阶段过早绑定数据库模型。
- **流程和验收边界**：不新增 `AI_TESTING.md` / `CODE_REVIEW.md` 默认文件；AI 生成代码验收要求合并进 `docs/TESTING.md` 模板，命名一致性通过 `docs/NAMING.md` 承接，并在团队协作、能运行起来、方便测试和交接场景中自动推荐。
- **安全和 AI 安全边界**：不新增“安全与合规层”常驻模块；`docs/SECURITY.md` 和 `docs/AI_SAFETY.md` 改为条件生成文件，不显示为默认勾选卡。后端 / 全栈 / Agent / RAG 生成 `docs/SECURITY.md`，Agent / RAG 额外生成 `docs/AI_SAFETY.md`；`PRIVACY.md` 和 `LICENSE` 继续按用户数据和开源分发意图再生成。
- **Runtime 轻量化修正**：`示例` 级 Prompt / Agent 资产只生成 `prompts/` 示例，不再让页面原型默认带出 `agents/` 和 `tools/` 目录；完整 Agent / RAG 方案仍按 Runtime 预设生成完整能力包。
- **SectionBlock 间距规范**：新项目和老项目的“标题 + 下方选项 / 表单 / 结果”已统一为 `SectionBlock`；向导题目到选项固定 8px，标题到内容固定 16px，代码来源选项改为真实 grid，不再堆叠贴合。
- **DocumentGrid 文档网格**：项目文档区域新增 `DocumentGrid` 规范；必选文件和推荐文件最多 3 列，空间不足自动退到 2 / 1 列，分组标题跨整行。
- **推荐文档三列节奏**：`optional-grouped` 推荐文档网格在桌面固定三列；即使某组只有 2 张卡，也只占前两列，第三列留空，不再把卡片拉成半屏宽。
- **文档预览入口**：`DocumentGrid` 选择方案一，预览入口为卡片右上角浮动 `i-eye` 图标按钮，按钮尺寸 22px、图标 14px，圆角与 checkbox 一致使用 `--radius-xs`；默认态只显示深色图标，无外框无底色，hover 时才出现轻量底色；标题/路径为按钮预留空间，描述文案恢复正文列宽度；已移除文档卡片和图谱查看中的眼睛 emoji。
- **新项目写入保护**：新项目入口改为“选择目录并生成骨架”；先做写入前安全检查并生成骨架方案，同名文件默认写 `.template.md`，相似命名资料可确认合并，原文件保留。
- **自动化引擎**：
    - `auto-reflect.sh`: 差异提取与教训写入。
    - `optimize-rules.sh`: 规则修剪。
    - `consolidate-memory.sh`: 自动压缩 `HANDOFF.md` 流水账到 `CHANGELOG`。
- **项目关系图内核**：新增 `scripts/build-project-graph.sh`，输出 `.project-os/graph/project-graph.json`，用于静态识别文件层级、引用关系和 `.ai/rules` 映射。
- **报告页交互降级**：`index.html` 和模板页已补齐内嵌浏览器 fallback；无 CDN / 无目录写入 / 无剪贴板权限时，下载模板、复制命令和受限能力提示都不会静默失败。
- **一键中控台**：打造根目录 `./kit` 工具，实现所有脚本的一键交互。

## 📊 当前仓库自检
- **上下文完整度**：100/100
- **工程成熟度**：70/100 (缺失真实的 npm 环境配置，符合脚本类项目预期)

## ⚠️ 风险与阻塞
- **模型敏感度**：自动反思脚本 (`auto-reflect.sh`) 在不同模型下的总结质量可能存在差异，需抽样微调 Prompt。
- **测试提示**：`tests/run-tests.sh` 仍会提示 `.env.local` 的 `DEEPSEEK_API_KEY` 为空；截图回归在找不到浏览器时会跳过 bitmap capture，但 marker 检查已通过。

## 🎯 下一步建议
1. **样本校准**：找一个真实的老项目运行 `./kit`，观察 v0.3 模型给出的“缺口建议”是否合理。
2. **逻辑增强**：优化 `check-ai-project.sh` 对非 JS 项目的 Lint/Test 探测能力。
3. **写入方案校准**：用已有 README / PRD / CLAUDE.md / 交接.md 的真实目录测试相似命名匹配是否准确。
