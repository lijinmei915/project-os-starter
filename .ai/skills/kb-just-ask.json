{
  "schemaVersion": "skill.v0.1",
  "name": "kb-just-ask",
  "description": "知识驱动问答：读知识注册表，按 use_when 语义定位该读的文件，由 AI 匹配、读取、溯源回答。P1 参考实现。",
  "trigger": ["kb问", "查知识", "知识问答", "这个项目怎么", "just ask"],
  "phase": "P1",
  "knowledge": {
    "requires": [],
    "optional": ["*"]
  },
  "input": {
    "args": [
      {
        "name": "question",
        "description": "要问的问题（自然语言）",
        "required": true,
        "example": "项目用什么前端框架，怎么启动"
      }
    ]
  },
  "output": {
    "stdout": "给 AI 的知识地图 prompt：列出所有知识条目(id/teaches/useWhen)，AI 按问题匹配 1~3 个文件后读取并溯源回答"
  },
  "sideEffects": [
    { "type": "none", "description": "只读操作，不写文件" }
  ],
  "action": {
    "type": "shell",
    "command": "bash scripts/kb-just-ask.sh ${args.0}"
  }
}
