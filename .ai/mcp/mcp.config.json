{
  "_note": "MCP 服务配置。project-os-analyzer 是未来自建 MCP server 的占位（P4 实现）。当前 P3 阶段通过 CLI skill 接入真实工具，MCP 层在 P4 编排时补齐。",
  "mcpServers": {
    "filesystem": {
      "_desc": "官方 filesystem MCP：让 AI 通过标准 MCP 协议读写项目文件，比直接 bash 更结构化",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "env": {}
    },
    "project-os-analyzer": {
      "_desc": "占位：Project OS 自建 MCP server，P4 阶段实现，届时封装 kb-* skill 为标准 MCP tools",
      "_status": "pending-P4",
      "command": "npx",
      "args": ["-y", "@project-os/mcp-server", "--dir", "."],
      "env": {}
    }
  }
}
